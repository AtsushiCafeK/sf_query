# 変更履歴

このファイルは各バージョンで何が変わったかを記録する。
書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に従う。

## [1.2.0] - 2026-09-01

「設定の変更だけで対応できる」範囲を実際に検証したところ、3つの穴が見つかったため
修正し、あわせて自動テストを整備した。

### 修正
- `ringi.fields.title` の綴り間違いが検証をすり抜け、**ダウンロード時に HTTP 500**
  になっていた。設定検証で検出するようにした
- **`ringi.attachment_type` が実装から一度も参照されておらず、設定を変えても無視されていた**。
  旧Attachment形式（`Attachment` / `Body`）の取得経路を実装し、設定で切り替わるようにした
  （※旧形式は実データでの検証が未了。0件になる場合は `files` に戻す）

### 追加
- 動作は止まらないが意図しない結果になりうる設定を、**警告**として画面に表示
  （件名が `columns` に無くフォルダ名がIDだけになる場合、旧Attachment形式を選んだ場合）
- **pytest による自動テスト（58件）**。Salesforceに接続せず実行できる。
  SOQL組み立て、インジェクション拒否、設定の検証と警告、連番付与と並び順、
  フォルダ分け／平置き、一覧表CSV、添付形式の切り替えを網羅。
  テストは `config.example.yaml` を土台にするため、配布している設定例の妥当性も同時に検証される

### 変更
- `sf_client.download_blob` を任意のオブジェクト/バイナリ項目に対応する形に一般化

## [1.1.0] - 2026-08-27

### 追加
- **設定ミスの原因を画面に表示**するようにした。`config.yaml` の書き間違いが
  エラー画面（HTTP 500）や無反応にならず、原因が画面に出る
  - YAMLの書式エラーは**行番号付き**で表示
  - 型・演算子・項目名の不正、`select` の `options` 欠落、`name` の重複を検出
  - キー名の綴り誤り（`filters` → `filter` など）を「見慣れない項目」として警告。
    従来は**エラーも出さずに検索欄が消える**ため気づきにくかった
  - 設定ファイル未作成時はコピー元（`config.example.yaml`）を案内
- `.gitattributes` を追加し、リポジトリ内の改行コードを LF に固定

### 変更
- `.claude/`（Claude Code のローカル作業ファイル）をGit管理対象から除外

## [1.0.0] - 2026-08-27

OAuth 2.0 での接続と TeamSpirit 本番環境での動作を確認し、正式版とした。
本番への適用にコード変更は不要で、設定ファイルのAPI名の差し替えのみで動作した。

### 修正
- `config.example.yaml` に `auth_mode` の記載が漏れていた。これをコピーした利用者は
  設定が既定の `cli` にフォールバックし、Salesforce CLI が無い環境では動かないうえ、
  本番用のOAuthへ切り替える方法が設定ファイルから読み取れない状態だった

### 変更
- 配布時の既定の認証方式を `oauth`（本来の使い方）に変更
- README / OAuth手順書を本番検証済みの内容に更新

## [0.9.0] - 2026-08-27

初回リリース。開発org上の稟議書モック（`Ringi__c`）で全機能を検証した状態。

### 追加
- **2つの検索モード**
  - 条件を選ぶ … レコードの種類・ステータスをプルダウン、期間をカレンダーで指定
  - SOQLを直接入力 … 任意のSOQLを貼り付けて実行（読み取り専用・`Id` 必須）
- **添付ファイルの一括ダウンロード**
  - 一覧と同じ並び順の連番（`001_`, `002_` …）を付与し、印刷した紙の順番と
    突き合わせやすくした
  - 配置を設定で切替（レコードごとのフォルダ／平置き）
  - 一覧表（CSV）を同時出力。Excelで開けるようBOM付きUTF-8。添付が無いレコードも
    「添付なし」として残す
  - ZIP化
- **検索フィルタの宣言化** … `config.yaml` の `ringi.filters` に書くだけで
  画面の入力欄と検索条件の両方が増える（Python・HTMLの変更不要）
- **OAuth 2.0 認可コードフロー + PKCE** … 画面内の「Salesforceに接続」ボタンから
  利用者ごとにログイン。トークンはCookieに保存せず、期限切れは自動更新、
  切断時は失効（revoke）
- **sf CLI パススルー**（開発用）… 接続アプリ不要で試せる代替の認証方式
- UIをダークモード専用に統一（全要素で WCAG AA 4.5:1 以上）
- pyenv + Poetry による環境管理

[1.2.0]: https://github.com/AtsushiCafeK/sf_query/releases/tag/v1.2.0
[1.1.0]: https://github.com/AtsushiCafeK/sf_query/releases/tag/v1.1.0
[1.0.0]: https://github.com/AtsushiCafeK/sf_query/releases/tag/v1.0.0
[0.9.0]: https://github.com/AtsushiCafeK/sf_query/releases/tag/v0.9.0
